export const runCodeParams = {
  code: 'import sys; print(sys.version)',
}

export const runCodeWithXSuccessParams = {
  code: 'import sys; print(sys.version)',
  xSuccess: 'shortcuts://run-shortcut?name=HandleResult',
}
