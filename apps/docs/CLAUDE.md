# CLAUDE.md - Protocol Launcher Docs

Use `AGENTS.md` in this directory as the primary docs instruction file.

Also follow the root `AGENTS.md` for workspace, package, and release rules.

## Claude-Specific Behavior

- Prefer small, reviewable docs changes.
- Keep English and Chinese docs updates paired when the local `AGENTS.md` requires it.
- After docs changes, run the most relevant docs build/check from `AGENTS.md` when practical.
- Keep shared docs guidance in `AGENTS.md`; keep this file as a lightweight Claude entrypoint.
