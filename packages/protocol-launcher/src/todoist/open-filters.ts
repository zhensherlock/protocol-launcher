/**
 * Open Todoist filters view (Mobile only).
 *
 * @returns Todoist filters URL.
 * @example
 * openFilters()
 * // => 'todoist://filters'
 * @link https://developer.todoist.com/api/v1/#tag/Url-schemes
 */
export function openFilters() {
  return 'todoist://filters'
}
