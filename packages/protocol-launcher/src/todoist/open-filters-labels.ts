/**
 * Open Todoist Filters & Labels view (Desktop only).
 *
 * @returns Todoist filters-labels URL.
 * @example
 * openFiltersLabels()
 * // => 'todoist://filters-labels'
 * @link https://developer.todoist.com/api/v1/#tag/Url-schemes
 */
export function openFiltersLabels() {
  return 'todoist://filters-labels'
}
