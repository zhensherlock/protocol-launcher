/**
 * Open Todoist Today view.
 *
 * @returns Todoist today URL.
 * @example
 * openToday()
 * // => 'todoist://today'
 * @link https://developer.todoist.com/api/v1/#tag/Url-schemes
 */
export function openToday() {
  return 'todoist://today'
}
