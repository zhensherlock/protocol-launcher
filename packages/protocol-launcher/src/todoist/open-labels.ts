/**
 * Open Todoist labels view (Mobile only).
 *
 * @returns Todoist labels URL.
 * @example
 * openLabels()
 * // => 'todoist://labels'
 * @link https://developer.todoist.com/api/v1/#tag/Url-schemes
 */
export function openLabels() {
  return 'todoist://labels'
}
