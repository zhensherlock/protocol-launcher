/**
 * Open Todoist app to the default view.
 *
 * @returns Todoist open URL.
 * @example
 * open()
 * // => 'todoist://'
 * @link https://developer.todoist.com/api/v1/#tag/Url-schemes
 */
export function open() {
  return 'todoist://'
}
