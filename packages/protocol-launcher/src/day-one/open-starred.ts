/**
 * Open Day One starred entries.
 *
 * @returns Day One starred entries URL.
 * @example
 * openStarred()
 * // => 'dayone://starred'
 */
export function openStarred() {
  return 'dayone://starred'
}
