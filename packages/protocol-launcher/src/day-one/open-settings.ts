/**
 * Open Day One settings.
 *
 * @returns Day One settings URL.
 * @example
 * openSettings()
 * // => 'dayone://preferences'
 */
export function openSettings() {
  return 'dayone://preferences'
}
