/**
 * Open settings in Codex.
 *
 * @param payload Open settings definition.
 * @returns Codex open settings URL.
 * @example
 * openSettings()
 * // => 'codex://settings'
 */
export function openSettings() {
  return 'codex://settings'
}
