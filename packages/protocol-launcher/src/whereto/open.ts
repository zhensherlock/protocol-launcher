/**
 * Open Where To?.
 *
 * @returns Where To? open URL.
 * @example
 * open()
 * // => 'whereto://'
 * @link https://www.futuretap.com/api/whereto
 */
export function open() {
  return 'whereto://'
}
