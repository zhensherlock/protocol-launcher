/**
 * Add a new script in Scriptable.
 *
 * @returns Scriptable add script URL.
 * @example
 * addScript()
 * // => 'scriptable:///add'
 * @link https://docs.scriptable.app/urlscheme/
 */
export function addScript() {
  return 'scriptable:///add'
}
