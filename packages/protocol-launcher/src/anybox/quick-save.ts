/**
 * Open Quick Save on macOS.
 *
 * @returns Anybox quick-save URL.
 * @example
 * quickSave()
 * // => 'anybox://quick-save'
 * @link https://anybox.app/url-schemes
 */
export function quickSave() {
  return 'anybox://quick-save'
}
