/**
 * Open Stash Box on macOS.
 *
 * @returns Anybox stash-box URL.
 * @example
 * stashBox()
 * // => 'anybox://stash-box'
 * @link https://anybox.app/url-schemes
 */
export function stashBox() {
  return 'anybox://stash-box'
}
