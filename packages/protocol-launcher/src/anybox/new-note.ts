/**
 * Open the New Note screen on iOS.
 *
 * @returns Anybox new-note URL.
 * @example
 * newNote()
 * // => 'anybox://new-note'
 * @link https://anybox.app/url-schemes
 */
export function newNote() {
  return 'anybox://new-note'
}
