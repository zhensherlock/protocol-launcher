/**
 * Open the Photos picker on iOS.
 *
 * @returns Anybox photos URL.
 * @example
 * photos()
 * // => 'anybox://photos'
 * @link https://anybox.app/url-schemes
 */
export function photos() {
  return 'anybox://photos'
}
