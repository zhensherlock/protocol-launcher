/**
 * Show or hide Anydock on macOS.
 *
 * @returns Anybox toggle-anydock URL.
 * @example
 * toggleAnydock()
 * // => 'anybox://toggle-anydock'
 * @link https://anybox.app/url-schemes
 */
export function toggleAnydock() {
  return 'anybox://toggle-anydock'
}
